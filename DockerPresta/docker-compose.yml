version: '3.9'

services:
  mysql:
    image: mysql:5.7
    container_name: prestashop-db
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: prestashop
      MYSQL_DATABASE_PASSWORD: prestashop
      MYSQL_ROOT_PASSWORD: prestashop
      MYSQL_TCP_PORT: 3307
    volumes:
      - ./mysql-data:/home/admin/Documents/composetest/data:delegated
    ports:
      - 3307:3307
    expose:
      - 3307
    networks:
      - prestashop
  
  presta:
    image: prestashop/prestashop:1.7
    container_name: prestashop
    volumes:
      - ./presta:/home/admin/Documents/composetest/data:delegated
    environment:
      DB_SERVER: mysql
    ports:
      - 8080:80
    networks:
      - prestashop

networks:
  prestashop:

